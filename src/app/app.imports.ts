import { RouterOutlet } from '@angular/router';

import { Navbar } from '@core/navbar/navbar';

export const NG_MODULES = [RouterOutlet];
export const EXTERNAL_MODULES = [];
export const CUSTOM_MODULES = [Navbar];
